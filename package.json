{
  "name": "mnemra-dev",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "check": "astro check",
    "verify": "node tests/verify/run.mjs",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.2.2",
    "astro": "^6.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "@playwright/test": "^1.44.0",
    "cheerio": "^1.0.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.95.0"
  },
  "engines": {
    "node": ">=22"
  },
  "overrides": {
    "vite": "^7"
  }
}
